# JRE (Java Runtime Environment)

The JRE is what you need to run Java programs.

It contains:

JRE = JVM + Java standard libraries

# Example libraries:

java.lang
java.util
java.io
java.net

# ! But it cannot compile Java code.

- because the compiler is not included.


